[package]
name = "node_graph_macros"
version = "0.1.0"
edition = "2021"
description = "Derive macros for the Task 40 node graph framework (ScriptNode / AnimationNode → NodeDescriptor)."

[lib]
proc-macro = true

[dependencies]
syn = { version = "2", features = ["full"] }
quote = "1"
proc-macro2 = "1"
